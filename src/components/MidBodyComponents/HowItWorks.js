import React from "react"
import { Typography } from "@material-ui/core"
import { makeStyles } from "@material-ui/styles"

const useStyles = makeStyles({})

const HowItWorks = () => {
  return (
    <>
      <Typography> HowItWorks </Typography>
    </>
  )
}

export default HowItWorks
