import React from "react"
import { Typography } from "@material-ui/core"
import { makeStyles } from "@material-ui/styles"

const useStyles = makeStyles({})

const CourseCards = () => {
  return (
    <>
      <Typography> CourseCards </Typography>
    </>
  )
}

export default CourseCards
